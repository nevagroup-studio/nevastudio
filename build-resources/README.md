Drop the final desktop app icon here when ready:

- `icon.ico`: Windows executable icon used by Electron Builder.

Recommended:

- Multi-size `.ico` containing 16, 24, 32, 48, 64, 128, and 256 px.
- Keep the canvas square with transparent background.

Current build uses Electron's default icon until you provide `icon.ico`.
